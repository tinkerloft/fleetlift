apiVersion: v1
kind: ResourceQuota
metadata:
  name: sandbox-limits
  namespace: sandbox-isolated
spec:
  hard:
    requests.cpu: "16"
    requests.memory: 64Gi
    limits.cpu: "16"
    limits.memory: 64Gi
    count/jobs.batch: "20"
